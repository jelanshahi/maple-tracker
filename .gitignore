node_modules/
dist/
*.tsbuildinfo

# Next.js build output. next-env.d.ts is committed on purpose: it is generated,
# but pnpm typecheck is a gate and should not require a build to run first.
.next/

# Secrets. The service role key bypasses RLS - it never enters git.
.env
.env.local

# Supabase CLI local state
.supabase/

# OS noise
Thumbs.db
.DS_Store

# Supabase CLI scratch state
supabase/.temp/
